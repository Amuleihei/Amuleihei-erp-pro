<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.skyeye.post.dao.PostDao">

    <select id="queryPostList" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
            a.id,
            a.content content,
            a.ip ip,
            a.anonymity anonymity,
            a.city city,
            a.address address,
            a.type_id typeId,
            a.circle_id circleId,
            a.upvote_num upvoteNum,
            a.comment_num commentNum,
            a.create_id createId,
            CONVERT(a.create_time, char) createTime,
            a.last_update_id lastUpdateId,
            CONVERT(a.last_update_time, char) lastUpdateTime
        FROM
            wall_post a
        <where>
            <if test="holderId != '' and holderId != null">
                AND a.circle_id = #{holderId}
            </if>
            <if test="type != '' and type != null">
                AND a.type_id = #{type} OR a.create_id = #{type}
            </if>
            <if test="sqlExtract != '' and sqlExtract != null">
                ${sqlExtract}
            </if>
            <if test="keyword != '' and keyword != null">
                AND a.`name` like '%${keyword}%'
            </if>
            <if test="createId != '' and createId != null">
                AND a.`create_id` like '%${createId}%'
            </if>
        </where>
        ORDER BY a.create_time ASC
    </select>

</mapper>